---
import { resolvedResume } from '@/lib/resume'

import TerminalShell from './TerminalShell'

interface Props {
  lang?: 'zh' | 'en'
}

const { lang = 'zh' } = Astro.props
const resumeHref = resolvedResume(lang).href
---

<TerminalShell client:load {lang} {resumeHref} />
