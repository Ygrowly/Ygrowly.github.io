# Pi Agent 博文「15 张问题卡」组件改造 Spec

> 执行指令：阅读并严格执行本 Spec，直接完成仓库检查、组件实现、文章迁移、测试和页面验证。不要停留在方案阶段，也不要等待我逐项确认。仅当仓库中不存在目标文章，或原文缺失到无法确定 15 道题及其答案对应关系时，才停止并报告阻塞。

## 1. 背景

当前 Pi Agent 源码阅读文章中，第一部分先列出 15 个问题，后面的第三部分又逐题给出“面试标答、答题结构、实现证据、可能追问”。这两部分内容重复，文章目录也因此塞入大量 Q1～Q15 及其子标题，影响长文浏览。

需要将两部分合并成一组可复用的面试问题卡：正面用于自测，点击后翻面并展开，集中展示答案和源码证据。组件应自然融入当前博客的暖白背景、黑灰正文与绿色强调色，不能像独立后台组件，也不要沿用现有金色大卡片样式。

## 2. 目标状态

完成后，目标文章应形成以下阅读闭环：

```text
15 张问题卡建立母题
→ 沿正文主线理解源码
→ 回到问题卡复习与组织面试表达
```

页面表现为：

1. 第一部分改为“15 张问题卡，先测再读”。
2. 桌面端默认两列展示紧凑问题卡。
3. 点击某张卡后，该卡在原位置跨两列展开；同一时间只展开一张。
4. 正面展示题号、分类、问题、考察点和查看提示。
5. 背面展示原问题、面试标答、答题结构、实现证据、追问方向及关联正文入口（原文存在时）。
6. 背面按内容自然增高，禁止卡片内部滚动。
7. 移动端使用单列展开/收起，不强制执行 3D 翻转。
8. 原“第三部分：15 个问题逐题回答”删除，其有效内容完整迁入卡片，后续章节顺序和编号同步调整。
9. 右侧文章目录不再展示 Q1～Q15 以及卡片内部的“面试标答、实现证据、可能追问”等条目。
10. 抽象后的组件能够直接复用于 Claude Code、OpenCode 等后续源码阅读文章。

## 3. 修改范围

### 3.1 必须修改

- 自动定位包含“阅读源码前，先锁定 15 个问题”以及逐题面试答案的 Pi Agent 文章。
- 新增可复用的 `InterviewQuestionDeck` 与 `InterviewQuestionCard`，命名可遵循仓库现有规范，但职责必须等价。
- 建立结构化问题数据，迁移现有 15 道题的全部有效内容。
- 修改目标文章的章节结构、导语、目录表现和必要的内部链接。
- 补充或更新与组件行为有关的测试。
- 运行仓库现有的格式检查、类型检查、测试和构建。

### 3.2 可以修改

- 目标文章对应的 MD/MDX 文件。
- 博客的共享组件、组件样式、目录提取逻辑和测试文件。
- 为组件新增独立数据文件、类型文件或样式文件。
- 为保持章节跳转正确而调整目标文章的锚点。

### 3.3 不做

- 不重写 Pi Agent 文章的技术观点，不擅自扩充新的问题或答案。
- 不修改其他文章的正文内容。
- 不重新设计博客全局布局、导航、文章字体或右侧目录。
- 不加入答题正确率、登录、后端存储或跨设备同步。
- 不为动画引入重量级 UI/动画依赖；优先使用现有组件能力、React 状态与 CSS。
- 不使用固定高度截断长答案，不出现内部滚动条。
- 不使用金色、强阴影、厚边框、弹跳动画或强烈的扑克牌式翻转。

## 4. 实施前仓库检查

先检查仓库，再选择符合当前架构的最小实现路径：

1. 阅读仓库根目录及相关子目录中的 `AGENTS.md`、README 和开发说明。
2. 确认框架、包管理器、MD/MDX 渲染方式、样式体系、主题变量、目录生成方式和现有测试方案。
3. 使用文章标题和关键文案搜索目标文件，不依赖预设路径。
4. 找到第一部分的 15 个问题与第三部分的 15 组答案，建立一一对应清单；迁移前确认题号无缺失、无重复。
5. 搜索已有的 Card、Accordion、Tabs、TOC 排除规则、`prefers-reduced-motion` 等实现，能复用则复用视觉 token 和基础能力。
6. 检查仓库当前工作区状态，保留用户已有修改，不覆盖无关内容。

不要因为实际技术栈、文件名或目录结构与本 Spec 示例不同而停下来；应按仓库约定自行适配。

## 5. 信息结构与数据模型

至少支持以下字段；可按项目命名规范调整，但不得把文章内容硬编码进组件渲染逻辑：

```ts
export type InterviewQuestion = {
  id: string
  category: string
  question: string
  keyPoints: string[]
  answer: React.ReactNode | string
  answerStructure: string[]
  evidence: Array<{
    label: string
    href?: string
  }>
  followUps: string[]
  relatedSection?: {
    label: string
    href: string
  }
}
```

约束：

- `id` 使用稳定的小写锚点，例如 `q01`～`q15`；界面显示为 `Q01`～`Q15`。
- `category` 沿用原文已有分类；原文没有时，从问题所在章节提炼短分类，不能虚构新技术结论。
- `keyPoints` 为 3～5 个简短考察点，用来提示范围，不直接泄露完整答案。
- `answer` 必须保留原文中可以直接说出口的面试标答；允许使用 MDX/ReactNode 保留行内代码和链接。
- `answerStructure` 应为 3～5 个短步骤，例如“一句话结论 → 分层职责 → 依赖关系 → 设计收益”。
- `evidence` 保留原文中的源码路径、函数、类或官方资料链接；已有链接必须继续可点击。
- `followUps` 保留 2～4 个原文追问，不擅自生成与源码证据不一致的新答案。
- 原文已有对应的正文详解时，添加 `relatedSection`，跳转到现有章节锚点。

数据可以放在目标 MDX 中，也可以放在同目录数据文件中。优先选择最符合仓库现有内容组织方式、同时方便其他文章复用的方案。

## 6. 组件职责

### 6.1 `InterviewQuestionDeck`

负责：

- 接收完整问题数组。
- 管理当前展开题目，保证同一时间最多展开一题。
- 管理本次页面生命周期内的“已查看”状态；无需持久化到本地存储。
- 渲染 Q01～Q15 的紧凑题号导航。
- 管理桌面两列、展开卡跨列和移动端单列布局。
- 点击题号导航后，将对应卡片滚动到合理的可视位置并聚焦；使用平滑滚动时必须尊重 `prefers-reduced-motion`。

### 6.2 `InterviewQuestionCard`

负责：

- 渲染单题的正面与背面。
- 支持鼠标、触屏以及键盘 `Enter` / `Space` 操作。
- 正确暴露 `aria-expanded`、`aria-controls`、可读标签和焦点状态。
- 展开后保留题号、分类与原问题，不能让读者忘记当前题目。
- 提供清楚的“返回问题”操作，关闭后焦点仍落在该题卡片上。
- 卡片内容区使用自然页面滚动，禁止 `overflow: auto/scroll`。

如果仓库采用服务端组件或静态 MDX 渲染，只把 Deck/Card 的必要交互部分设为客户端组件，不要扩大客户端边界。

## 7. 视觉规格

### 7.1 总体原则

组件必须像当前文章排版的一部分：低装饰、轻边框、足够留白、正文优先。颜色首先读取博客现有 CSS 变量；只有缺少对应 token 时才使用近似值。

参考语义值：

```css
--iq-card-bg: var(--existing-article-or-card-bg, #fff);
--iq-card-text: var(--existing-foreground, #222320);
--iq-card-muted: var(--existing-muted-foreground, #70726d);
--iq-card-border: var(--existing-border, #e7e4dc);
--iq-card-accent: var(--existing-green-accent, #087a5b);
--iq-card-accent-soft: var(--existing-green-soft, #eef7f3);
--iq-card-radius: 14px;
```

如果网站已有深色模式，必须使用现有主题变量保证深色模式可读，不能只为浅色页面写死颜色。

### 7.2 正面

正面从上到下包含：

```text
Q01                                      架构分层

Pi 的三层主干分别解决什么问题，
pi-tui 为什么不属于主链路？

考察：分层边界 · 依赖方向 · 运行时与 UI 解耦

先回答，再查看标答 →
```

要求：

- 默认卡片高度约 `220～260px`，允许内容自然撑高，不能为追求绝对等高而截断问题。
- `Q01` 使用现有等宽字体与绿色强调色。
- 分类位于右上角，使用小号弱化文本。
- 问题是视觉主体，字号应低于文章二级标题，建议使用响应式 `clamp` 而非固定超大字号。
- 考察点使用普通文本与中点分隔，不渲染成大量彩色胶囊标签。
- 底部必须有明确点击提示，卡片 Hover 时边框转为浅绿色并上移不超过 `2px`。

### 7.3 背面 / 展开态

桌面端展开后占满 Deck 宽度，顶部保留：

- `Q01`
- 分类
- 原问题
- “返回问题”按钮

主体使用约 `60% / 40%` 的双栏：

- 左栏：面试标答、关联正文入口。
- 右栏：答题结构、实现证据、追问方向。

当可用宽度不足时自动改为单栏。源码路径、函数名和类名使用现有等宽字体；长路径允许换行，不能撑破容器。

“面试标答”等只是卡片内的视觉标签，不使用会被全局目录扫描的文章级标题元素。应使用普通元素配合恰当的语义和样式。

### 7.4 题号导航

Deck 导语下方显示 `[01] [02] ... [15]` 式紧凑导航，可换行：

- 未查看：灰色。
- 当前题：绿色边框和浅绿色背景。
- 已查看：绿色小点或等价的克制状态，不使用大面积实心色。
- 每个题号必须有可读的辅助标签，例如“跳到问题 1”。

## 8. 动画与响应式

### 8.1 桌面端

保留轻微“翻面”的感知，但重点是内容切换和原位展开：

1. 卡片轻微缩小至约 `0.985`。
2. 使用短距离 `rotateY` 或等价的翻面过渡切换内容。
3. 展开为整行宽度，背面内容淡入。
4. 总时长控制在约 `260～320ms`，不使用弹跳。

不要求为了精确复刻 3D 翻牌而建立复杂脆弱的定高结构。若 3D transform 与动态高度、无障碍或现有布局冲突，应优先保证自然高度和稳定布局，使用缩放 + 淡入 + 轻微旋转表达翻面感。

### 8.2 移动端

- 单列卡片。
- 点击后在原位置纵向展开，使用高度/透明度过渡或无动画切换。
- 不做强 3D 翻转。
- 页面自然滚动，不能出现横向溢出或卡片内嵌滚动。

### 8.3 减少动态效果

在 `prefers-reduced-motion: reduce` 下关闭翻转、平滑滚动和位移动画，只保留即时状态切换。

## 9. 文章内容迁移

### 9.1 第一部分

将原标题调整为：

```text
第一部分：15 张问题卡，先测再读
```

导语调整为含义等价的自然表达：

```text
这 15 个问题既是源码阅读入口，也是全文的覆盖检查。
先尝试回答，再翻面核对面试标答、答题结构与实现证据。
```

导语下方依次放题号导航与问题卡组。

### 9.2 第三部分及后续章节

- 将原第三部分 Q1～Q15 中的有效内容迁移到对应卡片。
- 确认每道题的面试标答、答题结构、证据和追问均已迁移后，再删除原重复章节。
- 原第三部分删除后，按文章现有编号体系顺延后续章节，不允许出现编号跳跃或正文仍引用旧编号。
- 全文搜索旧标题、旧锚点和“见第三部分”等引用并修正。
- 如果外部链接可能依赖旧 Q1～Q15 锚点，尽量在卡片根节点保留兼容锚点；不要为了目录展示重新引入文章标题。

### 9.3 目录

最终全局目录只保留文章主线章节，例如：

```text
第一部分：15 张问题卡，先测再读
第二部分：一次请求怎样穿过三层
第 3 章：pi-ai 如何完成一次模型调用
第 4 章：Agent Loop 如何组织多次生成
……
```

卡片内的题号、面试标答、答题结构、实现证据和追问方向不得进入全局目录。优先通过正确的内容语义解决；仅在确有必要时修改 TOC 过滤逻辑，且不能破坏其他文章目录。

## 10. 状态与交互细节

- 初始状态下所有卡片关闭。
- 点击关闭卡片：展开该卡并标记已查看。
- 点击另一张卡：关闭原卡并打开新卡。
- 点击已打开卡的“返回问题”：关闭该卡。
- 打开卡片时不自动把页面跳到文章顶部，也不造成明显布局闪烁。
- 从题号导航跳转时，应让对应卡片完整或大部分出现在视口中。
- 浏览器前进/后退、刷新后无需恢复卡片状态。
- 不把“已查看”误称为“已掌握”或“回答正确”。

## 11. 测试要求

根据仓库现有测试栈补充最小但有效的测试。至少覆盖：

1. 渲染 15 道题，题号和问题均可见。
2. 初始无展开题。
3. 点击或键盘操作可以展开题目并显示答案。
4. 同时只能展开一道题。
5. 返回问题可以关闭当前题。
6. 展开后 `aria-expanded` 等无障碍状态正确。
7. 已查看状态在本次组件生命周期内生效。
8. 卡片内部不存在会导致长答案内嵌滚动的样式。
9. 目标文章构建后不再产生 Q1～Q15 及卡片子区域的全局 TOC 项。

如果项目没有适合的组件测试基础设施，不要为了本功能引入一整套重型测试框架；改用当前仓库已有的测试方式，并通过构建和浏览器验证补足。

## 12. 页面验收

完成实现后启动本地预览，至少检查以下视口：

- 桌面：约 `1440 × 900`，包含右侧文章目录。
- 移动端：约 `390 × 844`。

逐项确认：

- 两列卡片没有挤压正文或覆盖右侧目录。
- 展开卡跨两列且内容完整，没有内部滚动条。
- 连续打开 Q01、Q02、Q15 时始终只有一个展开。
- 题号导航可正常跳转，当前/已查看状态正确。
- 键盘可以完成打开、关闭和题号导航操作，焦点轮廓可见。
- 关闭动画后功能仍完整。
- 移动端无横向滚动，代码路径能换行。
- 浅色模式与仓库已有深色模式（若存在）均可读。
- 全局目录只显示主线章节，不再被卡片内容污染。
- 原 15 题的技术内容和链接没有在迁移中丢失。

如环境具备浏览器或截图能力，保存桌面端正面、桌面端展开态、移动端展开态三张验收截图；截图属于验证产物，不必提交到仓库，除非仓库已有视觉回归约定。

## 13. 必须执行的验证

自动识别并运行仓库已有命令，至少包括：

```text
格式检查 / lint
类型检查
相关测试或全量测试
生产构建
本地页面预览与交互验收
```

不得只因开发服务器能启动就判定完成。若仓库已有命令存在与本次修改无关的失败，应保留完整证据，说明本次相关验证是否通过，不得静默忽略。

## 14. 完成标准

以下条件全部满足才算完成：

- [ ] 已实现可复用的 Deck/Card 组件，而不是只为该文章写死 DOM。
- [ ] 15 道题数据完整且一一对应，无缺号、重复或内容丢失。
- [ ] 第一部分和原逐题回答部分已完成合并，重复内容删除。
- [ ] 组件视觉与当前博客一致，没有金色卡片或独立后台感。
- [ ] 桌面两列、单卡跨列展开和移动端单列均正常。
- [ ] 长答案自然撑高，无卡片内部滚动。
- [ ] 全局目录只保留主线章节。
- [ ] 鼠标、触屏、键盘和减少动态效果模式均可用。
- [ ] 原有源码证据与内部/外部链接可用。
- [ ] 格式、类型、测试和生产构建均已实际执行并报告结果。
- [ ] 未修改无关文章，未覆盖用户已有改动。

## 15. 最终交付报告格式

完成后用简洁报告交付：

```text
## 实现结果
- 组件与文件位置
- 文章结构迁移结果
- 关键交互与响应式结果

## 内容核对
- 15/15 题迁移情况
- 原第三部分删除及章节编号调整情况
- TOC 与链接核对情况

## 验证证据
| 命令/检查 | 结果 |
|---|---|
| lint | ... |
| typecheck | ... |
| tests | ... |
| build | ... |
| desktop/mobile QA | ... |

## 剩余问题
- 没有则写“无”
- 如有，仅列真实阻塞、非本次修改导致的既有失败或明确的后续建议
```

不要只给出“已实现”结论，必须同时给出实际运行的验证命令、结果和必要的页面验收证据。
